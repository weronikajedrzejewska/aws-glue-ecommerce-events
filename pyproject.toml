[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["airflow"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
markers = [
  "integration: tests covering multiple project modules end-to-end"
]
